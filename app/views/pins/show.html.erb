<h1> <%= @pin.name %></h1>
<ul>
	<% @pin.articles.each do |article| %>
		<li><%= link_to article.title, article_path(article) %></li>
	<% end %>
</ul>
<div id="back">
	<%= link_to "Back", articles_path, id: 'back'%>
</div>